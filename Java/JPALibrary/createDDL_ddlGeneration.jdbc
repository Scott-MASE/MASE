CREATE TABLE LIBRARY (B_ID INTEGER NOT NULL, B_NAME VARCHAR(255), PRIMARY KEY (B_ID))
CREATE TABLE STUDENT (S_ID INTEGER NOT NULL, S_NAME VARCHAR(255), PRIMARY KEY (S_ID))
CREATE TABLE STUDENT_LIBRARY (Student_S_ID INTEGER NOT NULL, books_issued_B_ID INTEGER NOT NULL, PRIMARY KEY (Student_S_ID, books_issued_B_ID))
ALTER TABLE STUDENT_LIBRARY ADD CONSTRAINT FK_STUDENT_LIBRARY_Student_S_ID FOREIGN KEY (Student_S_ID) REFERENCES STUDENT (S_ID)
ALTER TABLE STUDENT_LIBRARY ADD CONSTRAINT FK_STUDENT_LIBRARY_books_issued_B_ID FOREIGN KEY (books_issued_B_ID) REFERENCES LIBRARY (B_ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
